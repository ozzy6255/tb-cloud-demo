# 归档日志: 2025-12-09

## ✅ 今日完成 (Done)

### 1. 基础设施重构 (Infrastructure)
- **文件夹结构优化**: 实施了 "水电图" (Plumbing Diagram) 结构。
    - `src/`: 存放 `backend`, `frontend_new`。
    - `docs/`: 存放 `knowledge_base`, `archive`, `INDEX.md`。
    - `INDEX.md`: 创建为项目总索引。
- **文档清理**:
    - 删除了冗余的 `docs/ROADMAP.md` 和 `spec-kit` 模板。
    - 删除了 `docs/PROJECT_PRINCIPLES.md`，将其规则合并至宪法。
    - 统一更新了 `constitution.md` 为唯一法则文件。

### 2. 本地开发 (Local Development)
- **后端**: 使用 Python/FastAPI 替代了旧的 ASP.NET。
    - 实现了 `/products` 接口。
    - 解决了 CORS (跨域资源共享) 问题。
- **前端**: 使用原生 HTML/JS 实现了产品查询界面。
- **数据库**: 成功连接 Docker SQL Server (`TB_Restored`)。

### 3. 技术探索 (Exploration)
- **Antigravity**: 尝试了 AI Agent 浏览器自动化。
    - 结果：因开发环境网络限制 (Localhost 隔离) 未能成功连接。
    - 收获：深入理解了 Localhost 与云端 AI 的网络隔阂。

## 🚧 遗留问题 (Issues)
- **服务路径**: 文件移动后，需要确保重启 `uvicorn` 和 `http.server` 才能在正确路径运行 (目前终端仍在旧路径运行，可能需要重启)。
- **Cloud Deployment**: 第四阶段尚未开始，需解决 SQL Server 云端部署成本问题。

## ⏭️ 明日计划 (Next)
- **Phase 4: 云端部署**
    1.  **数据迁移**: 将 SQL Server 数据导出为 SQLite 或 MySQL 格式 (为了免费云部署)。
    2.  **服务选择**: 选择 Render 或 Tencent Cloud Serverless。
    3.  **上线**: 将代码推送至云端并验证公网访问。
