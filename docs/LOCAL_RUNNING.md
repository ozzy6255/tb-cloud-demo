# 本地运行完整应用指南

## 为什么需要本地运行?

当云端部署遇到问题时,本地运行可以:
- ✅ 立即验证功能是否正常
- ✅ 调试和测试新功能
- ✅ 不依赖网络和云服务

## 快速启动 (2 个终端)

### 终端 1: 启动后端
```bash
cd /Users/ozzymini/Documents/legacy_source/src/backend
export DB_TYPE=sqlite
../../venv/bin/uvicorn main:app --port 8002 --reload
```

**预期输出**:
```
INFO:     Uvicorn running on http://127.0.0.1:8002
INFO:     Application startup complete.
```

### 终端 2: 启动前端
```bash
cd /Users/ozzymini/Documents/legacy_source/src/frontend_vite
npm run dev
```

**预期输出**:
```
VITE v7.2.7  ready in 123 ms
➜  Local:   http://localhost:5173/
```

## 访问应用

打开浏览器访问: **http://localhost:5173**

您应该能看到:
- ✅ "TB 物流查询系统" 标题
- ✅ 双模式切换按钮
- ✅ Glassmorphism 设计
- ✅ 产品列表 (225 条)

## 测试物流查询

1. 点击 "物流追溯" 按钮
2. 输入测试追溯码: `0004089149583950`
3. 点击 "🔍 查询物流"
4. 查看物流信息卡片

## 环境变量说明

### 后端
- `DB_TYPE=sqlite` - 使用 SQLite 数据库 (app.db)
- 默认端口: 8002

### 前端
- `VITE_API_URL` - API 地址,默认 `http://localhost:8002`
- 开发服务器端口: 5173

## 常见问题

### Q: 后端启动失败 "Address already in use"
**A**: 端口 8002 被占用,杀掉旧进程:
```bash
pkill -f "uvicorn main:app"
```

### Q: 前端显示 "无法连接服务"
**A**: 确保后端已启动并运行在 8002 端口:
```bash
curl http://localhost:8002/api/health
# 应返回: {"status":"healthy","database":"connected"}
```

### Q: 数据库错误
**A**: 确保 `app.db` 存在:
```bash
ls -lh /Users/ozzymini/Documents/legacy_source/src/backend/app.db
# 应显示约 10MB 的文件
```

## 生产环境 vs 本地环境

| 维度 | 本地 | Render 云端 |
|------|------|------------|
| **访问地址** | localhost:5173 | tb-cloud-demo.onrender.com |
| **启动时间** | 秒级 | 30-60秒 (冷启动) |
| **数据库** | 本地 app.db | Docker 内 app.db |
| **前端** | Vite 开发服务器 | 编译后的静态文件 |
| **热更新** | ✅ 支持 | ❌ 需重新部署 |
